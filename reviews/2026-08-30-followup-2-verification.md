# Verificação do follow-up 2 (D1–D5 / R3) — base 1.24.6

- Data: 2026-08-30
- Revisor: Claude (auditor)
- Entrada: `reviews/2026-08-30-disposition-followup-verification-response.md`; commit
  `42625d1` (só `contact_center_base`: `group.py`, `queue.py`, 2 arquivos de teste)
- Evidência encadeada: `source-deploy-final` tree `8048beb8…` (01:40 UTC) →
  `tests-final-2` **314/314 + 590/590** (01:42) → `validate-final` (01:43). Lint limpo;
  590 testes contados (314 + 136 + 140); no diff de testes só um import não usado
  removido, nenhuma asserção.
- Lab (30/08 01:57 UTC, snapshot salvo): base `1.24.6`; **0 waiters, 0 profiles
  `failed`, 0 `dead` novos desde 01:00**; 2 `pending` recentes; 7 conexões saudáveis.

## Veredito

**Fechado.** D1 e D2 corrigidos com desenho melhor que o sugerido; a lacuna de cobertura
do R3 foi fechada com ciclo real; as refutações D3/D4/D5 procedem.

| Item                                              | Veredito          | Evidência                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ------------------------------------------------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **D1** profile `failed` → waiter sem convergência | ✅                | Nova lane `profile_failed` classifica como **`dead`** (não `unsupported`), com evidência congelada no `metadata_json` (profile, classe, revisão, tentativa, instante). Ordem forte de locks: `mail_channel FOR UPDATE SKIP LOCKED` → `group_profile FOR UPDATE` (recheque de `failed` sob lock; serialização concorrente vira retry do cron, nunca `dead` por snapshot velho) → ledger. Flush explícito dos 5 modelos antes do SQL. Teste `test_failed_group_profile_dead_letters_waiter_not_unsupported`. A tríade de classificação (escopo inexistente → `unsupported`; pausa/429 → `pending`; permanente/teto → `dead`) ficou coerente com o resto da fila. |
| **D2** flush do `savepoint()` no `try`            | ✅                | `with cr.savepoint() as sp:` **fora** do `try`; só o corpo é capturado, com `sp.rollback()` explícito; falhas de abertura/flush/fechamento propagam. Teste com escrita parcial + `SELECT 1 / 0` e SQL posterior no mesmo cursor.                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **R3** cobertura                                  | ✅                | `test_group_roster_defer_release_defer_preserves_lifetime_counters` (ciclo real acumulando `attempts` e `wait_count`, `first_group_roster_wait_at` preservado, release sem zerar, erro `Unsupported` antigo limpo antes do waiter novo, `paused`/`disconnected` recuperáveis).                                                                                                                                                                                                                                                                                                                                                                                 |
| **D3** segunda guarda ORM                         | Refutação procede | Odoo 16 roda em `REPEATABLE READ`: reler por ORM na mesma transação vê o mesmo snapshot; travar profile/binding depois do ledger inverteria a ordem do sync. A lane nova usa a ordem forte; para escopo arquivado, terminal-no-instante + requeue administrativo é aceitável.                                                                                                                                                                                                                                                                                                                                                                                  |
| **D4** `UnsupportedEventError` pegajoso           | Refutação procede | `_request_sync` grava `last_error_class=False` (`group.py:683`) e é chamado por `_defer_for_group_roster` **antes** de persistir o waiter, na mesma transação — o cron nunca vê waiter novo com erro antigo. Teste cobre.                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **D5** reset de `profile.attempts`                | Refutação procede | Orçamento por revisão do pull; o do evento é `inbox.attempts` — já confirmado na rodada anterior.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |

## Estado das pendências de todo o ciclo r3 → remediação → follow-ups

Nenhum defeito aberto de código nas frentes auditadas. Restam apenas: `git remote`
(única cópia local + bundle), e os itens de produto já aceitos como decisão
(retenção/PII do lab, recibos órfãos como `dead` após 12 tentativas, Meta bloqueada por
ações externas).
